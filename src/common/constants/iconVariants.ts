import { IconVariants } from "@common/typings"

export const iconVariants: IconVariants = {
  fill: "fill-white hover:fill-gold hover:text-gold",
  stroke: "stroke-white hover:stroke-gold hover:text-gold",
}
