import { FC } from "react";
import OverviewCard from "@lib/dashboard-overview/components/OverviewCard";

const Heading: FC = () => {
  return <OverviewCard theme={"light"} title={"Ticket information"} />;
};

export default Heading;
