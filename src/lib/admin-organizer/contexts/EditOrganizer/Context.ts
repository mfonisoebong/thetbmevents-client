import { EditOrganizerContextValues } from "@lib/admin-organizer/typings"
import { createContext } from "react"

export const EditOrganizerContext = createContext(
  {} as EditOrganizerContextValues
)
